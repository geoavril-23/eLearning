- [ ] Réécrire intégralement `eLearning/templates/admin/mes_formations.html` (HTML+CSS) pour appliquer la charte Premium teal/gold.
- [ ] Rendre chaque ligne de cours totalement cliquable via un lien englobant pointant vers `detail_cours_premium` avec `cours_id`.
- [ ] Ajouter hover effect (translateY(-2px), ombre, fond teinté) sur les lignes cliquables.
- [ ] Harmoniser le dark mode avec `body.dark-mode`.
- [ ] Vérifier que le `<style>` est valide et qu’il n’y a qu’un seul bloc CSS.

